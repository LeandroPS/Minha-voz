import styles from "../../../styles";
import styled from "styled-components/native";

const Title = styled.Text`
  color: ${styles["color-text"]};
  font-size: ${styles["font-sizing-7"]};
  font-weight: 700;
  margin: ${styles["spacing-3"]} 0;
`;

export default Title;
